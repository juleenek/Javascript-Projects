import { SENSORS_LIMIT } from "../main";

const easyDifficulty = {
  speed: SENSORS_LIMIT / 2
};

const mediumDifficulty = {
  speed: SENSORS_LIMIT / 3
};

const hardDifficulty = {
  speed: SENSORS_LIMIT / 4
};
